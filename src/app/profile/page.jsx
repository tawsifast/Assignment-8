import Quantity from '@/components/Quantity';
import React from 'react';

const ProfilePage = () => {
    return (
        <div>
            <Quantity/>
        </div>
    );
};

export default ProfilePage;